# 开局全亮但没有农民

## 现象

- 地图已全亮，说明开局入口已执行。
- 农民没出现，说明玩法 bootstrap 没跑到农民创建。

## 复用经验

- 看到“前半段初始化生效、后半段没生效”，优先查中间是否有同步重操作。
- JASS 模式下，全图 tile 遍历、批量 `SetTerrainPathable`、全单位遍历都可能触发 op-limit。
- 开局关键逻辑要先注册；地形覆盖、批量修正等可延后逻辑应分帧执行。
- 不要只修迷雾、rawcode、延迟或兜底；本次根因是同步地形覆盖中断了后续 bootstrap 注册。
